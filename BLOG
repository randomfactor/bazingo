### 26 November 2016

> nvm install v6.9.1
> nvm use v6.9.1

### 27 November 2016

> express --view=jade bazingo
# http://expressjs.com/en/starter/generator.html

. .gitignore
# https://github.com/github/gitignore/blob/master/Node.gitignore

> gedit BLOG
# this file. this one right here. the one you're reading now.

> git init
> npm init
> mv app.js server.js    # less confusion when adding aurelia

# copy/merge aurelia 1.1 files into project
> npm install
> npm install -g jspm gulp
> jspm registry config github
> jspm install -y

# interact.js
> jspm install aurelia-interactjs
> jspm install aurelia-event-aggregator

### 23 January 2017

# aurelia-interact passes the injected element to interact when "interact-draggable.bind"
# is specified in a view's attributes.

# in "game-piece.js", still need to figure out why interact-draggable::this.element is no
# the same object as GamePiece::this.piece.
